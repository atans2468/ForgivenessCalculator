import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.forgiveness.calculator',
  appName: 'ForgivenessCalculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
